import React from 'react'
import {View,Text,TouchableOpacity} from 'react-native'
export default function Deck({ title, numberOfCards, cards, id }) {
    return(
        <View>
            <Text>Here we are in the Deck view</Text>
        </View>
    )

}